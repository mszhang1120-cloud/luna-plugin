{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/tdd-guard.sh"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'input=$(cat); echo \"$input\" | grep -qE \"\\\\$CLAUDE_TOOL_INPUT.*(rm[[:space:]]+-rf|git[[:space:]]+push[[:space:]]+--force|git[[:space:]]+reset[[:space:]]+--hard|DROP[[:space:]]+TABLE)\" && { echo \"BLOCKED: 위험한 명령어가 감지되었습니다\" >&2; exit 1; }; exit 0'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'input=$(cat); echo \"$(date +%F%T) | $CLAUDE_TOOL_INPUT\" >> \"${CLAUDE_PROJECT_DIR}/.claude/audit.log\"' || true"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if [ -f \"${CLAUDE_PLUGIN_ROOT}/scripts/token-report.js\" ]; then node \"${CLAUDE_PLUGIN_ROOT}/scripts/token-report.js\" \"${CLAUDE_PROJECT_DIR}/.claude\" 2>/dev/null | tail -2; fi' || true"
          }
        ]
      }
    ]
  }
}
